node_modules/
dist/
android/
.DS_Store

# Bundled model weights are fetched at build time (scripts/fetch-model.mjs),
# not committed — they're large and exceed GitHub's 100 MB file limit.
public/models/
